import { Acronyms.To.StringPipe } from './acronyms.to.string.pipe';

describe('Acronyms.To.StringPipe', () => {
  it('create an instance', () => {
    const pipe = new Acronyms.To.StringPipe();
    expect(pipe).toBeTruthy();
  });
});
